/* @license GPL-2.0-or-later https://www.drupal.org/licensing/faq */
(function(Drupal,$){'use strict';Drupal.behaviors.gr_pride={attach:function(context,settings){const $elements=$('.anim-element',context);if($elements.length===0)return;const observer=new IntersectionObserver((entries)=>{entries.forEach((entry)=>{if(entry.isIntersecting)$(entry.target).removeClass('rise-to-top').addClass('fall-from-top');else $(entry.target).removeClass('fall-from-top').addClass('rise-to-top');});},{threshold:0.3});$elements.each(function(){observer.observe(this);});const el=context.querySelector('.home-banner-desc');if(el){const text=el.textContent;el.textContent='';let i=0;function type(){if(i<text.length){el.textContent+=text.charAt(i);i++;setTimeout(type,70);}}type();}}};})(Drupal,jQuery);;
